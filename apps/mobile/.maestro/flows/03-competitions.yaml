# 大会一覧
appId: ${APP_ID}
---
- runFlow: ../subflows/login.yaml
- tapOn:
    id: "tab-competitions"
- waitForAnimationToEnd:
    timeout: 5000
- takeScreenshot: ${OUTPUT_DIR}/${DEVICE_TAG}_03_competitions
