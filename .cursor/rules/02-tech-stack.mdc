---
alwaysApply: true
---

# 技術スタック

## プロジェクト構成

**モノレポ構成（npm workspaces）**

```
swim-hub/
├── apps/
│   ├── web/           # Next.jsアプリケーション
│   └── mobile/        # React Native + Expo
├── supabase/          # Supabase設定
│   ├── functions/     # Edge Functions（必要に応じて）
│   └── migrations/    # データベースマイグレーション
├── docs/              # ドキュメント
└── package.json       # ルートpackage.json（workspaces管理）
```

## Frontend (Web)
- **Framework**: Next.js 15 (App Router)
- **Language**: TypeScript 5
- **UI**: React 19 + Tailwind CSS 4
- **State**: Zustand (フォーム・フィルター・UI状態) + React Context (認証など)
- **Data Access**: Supabase Client (直接アクセス)
- **Form**: useState (React Hook Formは未使用)
- **Date**: date-fns 4
- **Testing**: Playwright (E2E)

## Backend
- **Database**: PostgreSQL (Supabase)
- **API**: Supabase Client (直接アクセス)
- **Auth**: Supabase Auth
- **Storage**: Cloudflare R2（プライマリ）、Supabase Storage（フォールバック）
- **Security**: Row Level Security (RLS)

## Mobile
- **Framework**: React Native + Expo
- **Language**: TypeScript
- **State**: Zustand (フォーム・フィルター・UI状態) + React Context (認証など)
- **Data Access**: Supabase Client (直接アクセス)
- **Navigation**: React Navigation
